<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="JQueryBoard">

	<!-- Result maps describe the mapping between the columns returned from 
		a query, and the class properties. A result map isn't necessary if the columns 
		(or aliases) match to the properties exactly. -->
	<resultMap id="JQueryBoardResult" type="JQueryBoard">
		<result property="bnum"        column="bnum" />
		<result property="nickname"    column="nickname" />
		<result property="title"       column="title" />
		<result property="mrank"       column="mrank" />
		<result property="content"     column="content" />
		<result property="ip"          column="ip" />
		<result property="hits"        column="hits" />
		<result property="category"    column="category" />
		<result property="filesize"    column="filesize"/>
		<result property="filename"    column="filename" /> 
		<result property="b_date"      column="b_date" /> 
	</resultMap>

	<select id="listJQuery" parameterType="JQueryBoard" resultMap="JQueryBoardResult">
    	<!-- select * from JQuery -->
    	select * from (select rownum rn, a.* from (select * from JQuery
    	order by bnum desc) a) where rn between #{start} and #{end}
	</select>
	
	<select id="listJQueryCategory" parameterType="JQueryBoard" resultMap="JQueryBoardResult">
    	select * from (select rownum rn, a.* from (select * from JQuery where category=#{category, jdbcType=VARCHAR}
    	order by bnum desc) a) where rn between #{start} and #{end}  	
	</select>
	
	<select id="JQueryRecent" resultMap="JQueryBoardResult">
    	select * from (select rownum rn, a.* from (select * from JQuery
    	order by bnum desc) a) where rn between 1 and 4
	</select>
	
	<select id="totalJQueryCategory" parameterType="String" resultType="int" >
		select count(*) from JQuery where category = #{category}
	</select>
	
	<insert id="JQueryInsert" parameterType="JQueryBoard">
		<selectKey order="BEFORE" keyProperty="bnum" resultType="int">
			select nvl(max(bnum),0)+1 from JQuery
		</selectKey>
		insert into JQuery
		values (
		#{bnum}, #{nickname, jdbcType=VARCHAR}, #{title}, #{mrank}, sysdate, #{content}, #{ip, jdbcType=VARCHAR}, 0, #{filesize, jdbcType=VARCHAR},
		#{filename, jdbcType=VARCHAR}, #{category} )
	</insert>
	
	<select id="JQueryDetail" parameterType="JQueryBoard" resultMap="JQueryBoardResult">
   		<![CDATA[select * from JQuery where bnum = #{bnum}]]>
 	 </select>
 	 
	<update id="JQueryUpdate" parameterType="JQueryBoard">
		update JQuery set
		title = #{title},
		content = #{content},
		category = #{category},
		filesize = #{filesize, jdbcType=VARCHAR},
		filename = #{filename, jdbcType=VARCHAR},
		ip = #{ip, jdbcType=VARCHAR}
		where
		bnum = #{bnum}
	</update>
	<delete id="JQueryDelete" parameterType="JQueryBoard">
		delete from JQuery where bnum = #{bnum}
	</delete> 
 	 
	<select id="totalJQuery" resultType="int" >
		select count(*) from JQuery
	</select>

</mapper>